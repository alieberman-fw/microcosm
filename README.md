<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="assets/logo-dark.svg">
    <img src="assets/logo-light.svg" width="110" alt="Microcosm logo">
  </picture>
</p>

<h1 align="center">microcosm</h1>

<p align="center">
  <strong>Simulate the market before you build for it.</strong><br>
  Agent-simulation platform for the built world.
</p>

<p align="center">
  <a href="#the-idea">The idea</a> ·
  <a href="#the-product-today">The product today</a> ·
  <a href="#architecture">Architecture</a> ·
  <a href="#build-checklist">Build checklist</a> ·
  <a href="#the-value">The value</a> ·
  <a href="#running-locally">Run it</a>
</p>

---

## The idea

Real estate is the worst-instrumented big-money decision domain in the economy. Land is bought once, product is entitled once, a tower is built once — all committed before a single buyer is surveyed. Feedback loops run 12+ months, decision-grade research costs $15–50K and takes six weeks per question, and the stakeholders who decide a project's fate — buyers, renters, neighbors, lenders, councils, LPs — never sit in one room.

**Microcosm puts them in one room.** It seeds thousands of persona-grounded AI agents — buyers, renters, neighbors, lenders, investors, and domain experts — that react, debate, change their minds, and converge (or dissent, on the record). Then it hands you the report, before a single dollar of capital is committed.

Two product modes:

| Mode | Question it answers | Example |
|---|---|---|
| **Consumer panels** | Will they pay? | Which floor plan, what unit mix, what price point, which amenity package |
| **Expert deliberation** | Will it work — and will they allow it? | Site go/no-go, entitlement rehearsal, policy wind-tunnel runs |

The bet under both: the $15–50K, six-week feasibility study collapses into a *run* — and whoever owns the calibration data that makes runs decision-grade owns the layer.

### Why now

- **The science matured.** Stanford's generative-agents line went from 25 agents in a sandbox town (2023) to 1,000-person replications hitting 85% of humans' own test-retest consistency (2024) to million-agent societies (2025). Agent-based modeling is 50 years old; LLMs just gave the agents the ability to read a floor plan and argue about it.
- **The economics flipped.** Per-unit intelligence deflates 10–40× a year. A thousand-agent metro run costs dollars, not grants.
- **The category is funded — and horizontal.** Aaru raised at a ~$1B headline; Simile ($100M Series A) employs the field's founding scientists. Nobody has verticalized for the built world, where decisions are the most illiquid and the stakes highest.

## The product today

A working multi-tenant app (internal preview, Fifth Wall Google accounts only) plus the scripted demo that serves as the product's design contract. What a signed-in user can do right now:

| Surface | What it does |
|---|---|
| **Conversations** (`/conversations`) | iMessage-style 1:1 and group chats (up to 20 personas). @mention with **autocomplete** to direct a question; with no mention, a Haiku router picks the right responder. Attach up to 8 images/PDFs per message (5MB each, ~20MB combined) — experts analyze the actual file contents. Per-participant **model toggle** (Haiku 4.5 default → Sonnet 5 → Opus 4.8), persisted per thread. Big rooms get a **roster panel** (scroll everyone, open full profiles, cycle model tiers). Rename or delete any thread from its hover menu; fresh threads with the same people are one click; history is durable and org-scoped. |
| **Agent Library** (`/personas`) | ~1,850 generated personas spanning the entire built-world taxonomy — every one with a name, age, metro, household, income, credentials, career backstory, standing positions, skills, and behavioral traits. Click any card for the full profile view. **Smart search** in plain language: a problem ("looking to build a data center") or a person ("under 40 homeowner") — an LLM pass turns the query into full-text terms + demographic filters. Custom personas: create, chat, delete (org-scoped). |
| **Monitoring** (`/monitoring`) | Every model call the org makes — surface, agent, model, tokens in/out, latency, status — with estimated spend. |
| **Dashboard** (`/dashboard`) | Simulation history (persistence is the product memory — and the future calibration substrate). |
| **Run screen** (`/sim/demo`) | The live-simulation UI (network graph + forum feed) replaying the demo's 46-event golden fixture — the contract the real engine must stream into. |
| **Live demo** (`/demo.html`) | The scripted four-stage walkthrough (Brief → Seed → Run → Read) on Site 47-A — static, self-contained, the design golden fixture. |

## Architecture

Three layers, one contract between them (the event schema in `CLAUDE.md` §6.2).

```
┌─ Next.js 15 (App Router, TS) on Vercel ─────────────────────────┐
│  UI (inline styles + CSS token design system, dark/light)       │
│  BFF route handlers: /api/converse · /api/personas/search       │
└──────────────┬───────────────────────────────┬──────────────────┘
               │ @supabase/ssr (RLS-scoped)    │ @anthropic-ai/sdk
┌──────────────▼──────────────┐   ┌────────────▼──────────────────┐
│  Supabase                   │   │  Anthropic API                │
│  Postgres + org RLS         │   │  Haiku 4.5 / Sonnet 5 /       │
│  Auth (Google, internal)    │   │  Opus 4.8 (see tiers below)   │
│  Storage (documents bucket) │   └───────────────────────────────┘
│  Realtime (run events) ·    │
│  pgvector (embeddings col)  │
└──────────────▲──────────────┘
               │ event schema §6.2 (posts, stage, presence, sentiment, convergence)
┌──────────────┴──────────────────────────────────────────────────┐
│  engine/ — Python FastAPI + swarms (simulation runs; skeleton)  │
└─────────────────────────────────────────────────────────────────┘
```

### Frameworks & libraries that do the heavy lifting

| Layer | Technology | Role |
|---|---|---|
| Web app | **Next.js 15** (App Router) + TypeScript + React 19, deployed on **Vercel** (GitHub → main auto-deploy) | All UI + backend-for-frontend route handlers |
| Data / auth / files | **Supabase**: Postgres with row-level security (every table org-scoped via `public.user_org()`), Auth (Google OAuth, internal-only; email sign-in disabled at the provider level), Storage, Realtime, pgvector | Multi-tenant persistence from day one — users, orgs, personas, conversations, documents, interactions |
| LLM access | **`@anthropic-ai/sdk`** (direct, no middleware) | Group-chat replies, routing, persona generation, smart-search query parsing |
| Simulation orchestration | **[swarms](https://github.com/kyegomez/swarms)** (Apache-2.0, self-hosted in `engine/` — Python FastAPI) | Multi-agent architectures behind the Microcosm interaction modes (below); the hosted Swarms API is a documented fallback only |
| Persona search | **Postgres full-text search** (generated `tsvector` + GIN) + a Haiku query-understanding pass + jsonb demographic filters via the `search_personas` RPC | "Under 40 homeowner" → age/tenure filters; "build a data center" → expanded professional term search |

### Group chats vs. simulations — how each runs

**Conversations (shipped)** are orchestrated in the Next.js route handler ([app/api/converse/route.ts](app/api/converse/route.ts)): parse @mentions → if none, a Haiku router reads the transcript tail and picks 1–2 responders → each responder gets its compiled persona system prompt ([lib/personas.ts](lib/personas.ts) `compilePersonaPrompt` — a versioned pure function), the rolling transcript, and any attachments as native image/document content blocks → replies generate sequentially so later speakers can react to earlier ones → every call is logged to `agent_interactions`.

**Simulations (engine v1, shipped)** run in-app ([lib/engine.ts](lib/engine.ts), TS orchestrator — §6.3 deviation; the Python/swarms service in `engine/` remains the scale path). The interaction modes users see map to swarms architectures:

| Microcosm mode | swarms architecture | Shape |
|---|---|---|
| **Agora** (default) | `GroupChat` / `InteractiveGroupChat` | Open-square deliberation, threads, @mentions — also powers "Take the Floor" |
| **Roundtable** | `RoundRobin` | Every voice each round |
| **Tribunal** | `DebateWithJudge` | Advocates argue, judge rules |
| **Chamber** | `LLMCouncil` | Independent takes → anonymized peer review → synthesis |
| **Jury** | `MixtureOfAgents` | Parallel verdicts, aggregated |
| **Desk** | `HierarchicalSwarm` | Director + section workers (report synthesis) |
| **Expedition** | `HeavySwarm` | Autonomous deep research packs |

### Round semantics — what "a round" means, per mode

The canonical table (also in `/docs` → Interaction modes; pinned by the offline test matrix in [tests/engine/modes.test.ts](tests/engine/modes.test.ts) — change all three together):

| Mode | One round = | Round ends when | Run ends when |
|---|---|---|---|
| **Agora** | One lead opens (rotating) + replies routed to **any post from the round** (weighted recent + contested) — focused: ≤6 replies · lively: ~1.5× leads · bustling: 2× leads | Reply budget spent → poll → interjections → votes | Rounds cap · positions stable 2× in a row (from round 3) · post budget |
| **Roundtable** | Every lead speaks once, in order — then a density-scaled **CROSSFIRE** half-round (lively: ½ panel · bustling: all) | Crossfire closes → poll → interjections → votes | Same as Agora |
| **Tribunal** | Up to 3 FOR arguments → up to 3 AGAINST rebuttals → **COUNTER** volley alternating benches (lively: 2 · bustling: 4) → judge's note | Judge rules → poll → interjections → votes | Rounds cap · post budget (always adversarial — no stability stop) |
| **Jury** | Every juror scores (blind in R1; sees the tally after) + code-computed TALLY — density never adds deliberation posts (verdict integrity IS the mode) | Tally posts → poll → interjections → votes | Scores stop moving (no juror shifts ≥1 point) · rounds cap · post budget |
| **Chamber** | *Phases, not rounds:* takes → blind review → chair synthesis (crowd reacts + votes after takes and after synthesis) | Phase completes | Choreography completes (never "converged") |
| **Desk** | *Phases:* assignment → section drafts → director's memo — no polls/bursts/votes | Phase completes | Choreography completes |
| **Expedition** | *Phases:* questions → research → analysis → alternatives → verify — no polls/bursts/votes | Phase completes | Choreography completes |

Every polled round closes the same way: **crowd poll** → **interjection burst** (a density-scaled crowd sample posts short reactions threaded under the round's posts — one batched call) → **vote sweep** (every lead + a crowd sample endorses ▲/rejects ▼ the round's arguments in character; persisted to `post_votes` with hover attribution). At lively/bustling, votes also land in **realtime** — a rotating voter sample reacts every 3rd post, pair-deduped against the closing sweep. The **DENSITY** run-config control (focused · lively · bustling) scales all of it, and the estimator prices it from the same functions the engine runs (`lib/run.ts` density helpers — engine and estimator can't drift). A poll cut off by a serverless slice ships its partial tally honestly; nothing is double-polled, double-burst, or double-voted on resume. The run's `stop_reason` (stability · rounds · budget · choreography) is persisted and shown in the report methodology.

### Anthropic models — who runs on what

Model ids live in config only ([lib/chat-models.ts](lib/chat-models.ts), `CLAUDE.md` §6.4) — never hardcoded in logic.

| Model | Tier | Where it runs today |
|---|---|---|
| **Haiku 4.5** (`claude-haiku-4-5`) | Lightweight & fast | Default for every chat reply (bump per-persona via the thread toggle) · group-chat responder routing · smart-search query parsing |
| **Sonnet 5** (`claude-sonnet-5`) | Balanced | "Balanced" chat tier · the persona foundry (`scripts/generate-personas.mjs`) · future: casting director (standard), verifier |
| **Opus 4.8** (`claude-opus-4-8`) | Frontier reasoning | "Frontier" chat tier · future: judges, casting (frontier), report synthesis |

### The persona pipeline

1. [`docs/persona-taxonomy.md`](docs/persona-taxonomy.md) — the master library: 37 horizontal categories, 12 asset-type build stacks, the tech/product-validation library, 12 modifier axes (~1,100 base personas).
2. [`scripts/generate-personas.mjs`](scripts/generate-personas.mjs) parses it into ~1,867 seats (household/demand cohorts get 2–3 demographic variants), batches 10 seats per Sonnet call, and writes full persona JSONs to Postgres as global library rows. Idempotent via `spec.seed_key` — re-run any time; only gaps generate.
3. Every persona is a **synthetic composite** — realistic demographics and career narratives, never a real individual. Fair-housing line: Microcosm simulates markets and decisions, never individual tenant/buyer screening.
4. Search: Haiku translates the user's natural-language query into websearch-syntax terms + structured filters (age bounds, tenure, kind) → `search_personas` RPC executes over the FTS index under RLS. The pgvector matching flow (casting-time library reuse) activates when an embeddings key is added — the `embedding vector(1536)` column is already in the schema.

## Build checklist

**This is the living tracker — every feature PR checks items off (or adds them) so the README always shows what exists and what's next.** Mirrors `CLAUDE.md` §11 phases.

### Phase 0 — Spec
- [x] `CLAUDE.md`: product / tech / design contract, persona taxonomy, swarms mode mapping

### Phase 1 — POC (real infrastructure, narrow features)
- [x] Landing site + interactive scripted demo (the design golden fixture)
- [x] Supabase from day one: full schema + org RLS, personal org per signup, Storage, signup trigger
- [x] Auth: Google OAuth locked to the Fifth Wall workspace; email sign-in disabled at the provider
- [x] App shell: sidebar nav, profile popover, theme toggle, settings, dashboard history
- [x] Run-screen golden fixture: `/sim/demo` replays the demo's 46 events through the real event schema
- [x] **Conversations**: persistent 1:1/group chats, @mentions, Haiku responder routing, fresh threads
- [x] **Attachments**: 8 images/PDFs per message analyzed natively; overlap-chip UX with +N expander/lightbox
- [x] **Monitoring**: every model call logged (`agent_interactions`) with estimated spend
- [x] **Persona library at scale**: ~1,850 taxonomy-generated personas with full demographics/backstories; profile view; LLM smart search; picker integration
- [x] **Per-participant model toggle** (Haiku 4.5 default · Sonnet 5 · Opus 4.8), persisted per thread
- [x] **Chat management**: rename/delete via hover ⋯ menu; rooms up to 20 personas with a roster panel (scroll the list, click through to full profiles, cycle each person's model tier); @mention autocomplete in the composer
- [x] **Library QC + speed**: every persona name unique library-wide (dedup pass + generator enforcement); LLM audit of demographics coherence (income/credentials/tenure/experience vs role + metro); two-phase search (instant full-text, AI refinement ~2s later); middleware session fast-path; same-name participants disambiguated end-to-end (typeahead-resolved mention keys, role-tagged transcript labels)
- [x] **Library filters + pagination**: filter rail (kind · category · age band · tenure · sort) with facet counts, composing with tabs and search; 24-per-page pagination with totals; LLM parse cached across pages
- [x] **Chat polish**: markdown rendering in replies (bold/headers/lists/code + @mention highlighting, dependency-free); sidebar list fills its height with SEE ALL → searchable `/conversations/history` (deep links reopen the exact thread); landing **Conversations** section ("Don't need a full simulation? Just ask the room.")
- [x] **Monitoring analytics**: filter rail (area · model · status · search), 14-day activity chart, spend-by-model and calls-by-area breakdowns, app-area labels, and expandable rows showing the conversation context — what was asked, what the agent replied, per-call spend, and a deep link into the thread
- [x] **Remix + full persona editor**: remix any persona (library or custom) into your own editable copy with a ⑂ REMIX tag and a clickable lineage chain (remix-of-remix keeps full ancestry, e.g. "Delores V. → Casey R. → Jordan P."); custom personas get every library field (tagline, demographics, skills, trait sliders) and are editable after creation; renames auto-rewrite story references; chat composer grows vertically as you type
- [x] **Clean location fields**: demographics store city and state separately (all 1,867 library rows migrated off "Kansas City, MO"-style duplication); the editor's CITY field is a verifiable typeahead over a curated US city dataset (✓ VERIFIED / UNLISTED hint) with a proper State select; displays recompose "City, ST"
- [x] **Streaming group replies + Home**: when a message addresses the whole group ("each of you"), everyone answers — no more silent third voice; replies stream in as each persona finishes with iMessage-style "X & Y are typing…" indicators; dynamic pages skip the client router cache so fresh conversations never vanish on back-navigation; new **Home** tab (house icon, default landing) with a getting-started checklist computed from real activity (clearable, re-enabled in Settings via `users.prefs`) that matures into a recent-activity dashboard
- [x] **"Build the room" participant browser** (`/conversations/new`): full-page browse over all personas with the same smart search + filter rail + pagination as the library, multi-select cards, and a sticky launch bar (selections persist across searches/pages, up to 20); linked from the picker ("BROWSE ALL WITH FILTERS →") and the empty state; also fixed the picker's crushed result rows (overflow-hidden grid items auto-min to zero — `gridAutoRows: max-content`)
- [x] Brief composer + corpus — `/sim/new` (problem statement, AI-suggested question chips, decision templates, success criteria) → `/sim/[id]` workspace; upload → Supabase Storage + Anthropic Files API + local parse/chunk (FTS substrate); "Test the corpus" answers with native citations + cached corpus prefix. **Architecture decision: whole documents in context (Files API + citations + prompt caching) for v1 — no embeddings; chunk retrieval activates for swarm-scale runs and the verifier**
- [x] Brief refinements: decision **shape** is inferred by the suggest pass and shown as an editable "READS AS · …" hint (template pills removed — report sections always come from questions + success criteria; the shape only picks the lead visual); question chips carry a one-line framing ("POWER TIMELINE — can 300MW interconnect inside 36 months?"); embeddings provider decided: **Vercel AI Gateway** (`AI_GATEWAY_API_KEY`, `openai/text-embedding-3-small` = 1536 dims matching the existing pgvector columns)
- [x] Brief polish: decision shape is now **fully silent** (classified into `brief.template`, zero UI — reports compose from questions + success criteria); re-suggest replaces prior AI chips instead of stacking (hand-added ones survive); success criteria became a styled bulleted list (type + Enter, accent markers, legacy free-text auto-migrates)
- [ ] Corpus depth: DOCX/XLSX parsing, per-doc viewer with citation deep-links, retrieval mode past the context budget (pgvector hybrid via the Vercel AI Gateway)
- [ ] Mode picker with animated per-mode explainer diagrams + per-mode live run visual grammar (CLAUDE.md §5 table: Agora network, Roundtable ring, Tribunal opposing benches + verdict scale, Chamber blind-review phases, Jury tally lanes, Desk org-tree, Expedition route) — ships with run-config + engine
- [x] **Casting Director** (demo Stage 02): one Sonnet-tier pass over brief + corpus → composition, recommended scale, recommended mode, 6–12 lead seats with exactly one adversarial seed; seats stream onto the card grid as they resolve — org custom personas matched first, then the 1,800-library (FTS), true gaps generated and saved back to your library; provenance tags, profile click-through, per-seat remove, re-cast with guidance; cast frozen into `sim_agents.spec_frozen` + `config.casting`
- [x] **Sim workspace experience**: casting hardened (plan cap 3,500 + bracket-stack JSON salvage in `lib/llm-json.ts` + silent retry — shared with the suggest pass); **casting theater** (canvas node/pulse animation + rotating status lines while the plan drafts; live cards carry frozen seat metadata so ADVERSARIAL tags render mid-cast); **auto-cast vs hand-pick** choice (SeatPicker modal over your personas + library smart search, zero tokens; also "+ ADD FROM LIBRARY" after any cast); **field notes** — no documents? write what you know, saved to the corpus through the same parse/chunk/Files-API pipeline (✎ rows); **GFM table rendering** in Markdown (corpus answers render as one block — citations no longer shatter tables); clickable stage rail; Test-the-corpus answers scroll in a capped container
- [x] Sim management: `/sim/new` keeps a localStorage **draft** (leave mid-setup, come back, DRAFT RESTORED · discard; cleared on create); dashboard cards get the hover **⋮ menu** — Edit brief & setup / two-step Delete (removes docs, chunks, storage, Files API objects, and the cast)
- [x] Population review: **panel-size presets** (FOCUSED 6 · STANDARD 10 · DEEP 15 · MAX 20, honored exactly by the caster; full-run crowd of 500+/1,000 residents arrives with PUMS + run config); **kind + provenance filter pills** with live counts (experts / consumers·residents / stakeholders / adversarial × your library / library / new-generated); grid scrolls internally past 12 cards; guidance runs in two modes — **RE-CAST ALL** or **ADD MORE** ("add two pool engineering experts" appends without touching the panel); monitoring rows carry sim context (**detail** jsonb + sim_id): expanded rows show cast mode, seats asked, guidance, corpus question, and an OPEN SIMULATION deep link
- [x] Swarm casting theater: the loading animation is now a live particle swarm with phased choreography synced to the director's status — SCATTER (drift in) → SWEEP (murmuration around a moving attractor) → CLUSTER (four discipline orbits) → ASSEMBLE (reforms as the panel ring with hub pulses) — looping with emergent dissolve/reassemble transitions; the swarm parts around the cursor (pixel-verified 640→0 in the repulsion zone); velocity tails, amber adversarial dot, reduced-motion fallback
- [x] Add-more feedback + scale semantics: ADD MORE shows an immediate SCOUTING ADDITIONS mini-swarm card, then one mini-swarm per incoming persona; the readout now reads FULL-RUN CROWD — SET AT RUN CONFIG (SOON) with an explainer tooltip so recommended scale is never confused with the ≤20 deliberation leads; casting inputs audited (richer 2-chunk/700-char corpus excerpts, composition↔seat-kind consistency rule)
- [x] Population controls (§3 composition surface): AUTO/EXPERTS/RESIDENTS/MIXED composition — forced pre-cast or via two-step re-cast; seven-mode selector with YOURS vs RECOMMENDED and live description update; editable full-run crowd counts (4–500 / 0–1,000) persisted with user_set flags for the engine; CrowdBand dot-field makes the whole population visible on the population stage
- [x] **Crowd materialization**: the crowd is real members, not just a number — GENERATE THE CROWD turns the scale into compact Haiku-tier personas (`crowd.generate`, 3-concurrent batches of 25, name-deduped, wrapper-salvage + zero-produce retry), streamed live into the band (lit dots = materialized) and stored as `sim_agents` rows with `seat.tier "crowd"` (persona_id null — never pollutes the library); crowds past 300 get a proportional representative sample (full scale at run); **BROWSE THE CROWD** roster modal — search, expert/resident pills, profile click-through, per-member remove; REGENERATE + COUNTS CHANGED staleness warning; expert crowd cues off expert leads only, residents are narrative-seeded until PUMS
- [x] **In-app docs** (`/docs`, sidebar entry): six interactive pages in the app's own grammar — What is Microcosm (five stages, principles) · Simulations & swarms (why panels beat one model, grounding layers, honest-instrument line) · Core concepts (clickable **kind × tier matrix**, glossary) · Interaction modes (all seven with **looping canvas diagrams** — `ModeDiagram`, reused by the run-config mode picker later) · Casting & population (auto vs hand-pick, re-cast vs add, crowd generate/browse/regenerate, live CrowdBand demo) · Getting started (conversations in 2 min, first sim in 15, library, monitoring); registry in `components/app/docs/content.tsx`; **convention: every user-facing feature PR updates its docs page** (CLAUDE.md §12)
- [x] Population numbers clarity: the control row is **POPULATION · TOTAL EXPERTS / TOTAL RESIDENTS** with a live `= N LEADS + M CROWD` readout that updates as you type, an explicit **APPLY** button (green border while dirty, SAVED ✓ on commit — no more silent blur-commit), and a plain hint that totals include the leads; one equation drives header, control row, crowd panel, band, and roster
- [x] Docs v2: three new pages — **Agents & the library** (what a persona is, the 1,800 library, customs, remix lineage), **Conversations & group chats** (@mention rules, ask-everyone, attachments, model tiers — with an app-styled chat vignette), **Monitoring & costs** (column-by-column, expandable rows, no-surprise-bills) — plus a **POPULATION MATH** vignette + step-by-step on the casting page; 9 pages total, vignettes render the app's own components in miniature
- [x] Recast & history polish: run-config panel hides while a re-cast is in flight (stale mode/warnings can't mislead); truncated casting plans are REJECTED instead of silently seating a partial panel (4/10 → clear retry/error); dashboard sim cards carry **run/report badges** (RAN · MODE · N POSTS, N REPORTS · Vn) with Open-the-run / View-report menu actions — the per-sim history entry point; **Monitoring pagination** (50/page over the filtered set, page resets on filter change)
- [ ] Population editor depth: per-seat regenerate/swap, persona sets
- [x] **Run configuration (§4.1)** on the workspace: seven **animated mode cards** (the docs' `ModeDiagram` doubling as the picker, per §5), rounds / max posts / sim days / speaker (Agora) / convergence / temperature / model tier / verifier — plain-language help on every control, persisted to `config.run` (clamped + validated in the config PATCH); **mode-fit flags** before launch (Tribunal side balance, Roundtable >12, Chamber N², no-crowd info); **pre-launch cost estimate** (rates + token shapes in `lib/run.ts`, never in components) with post/poll counts
- [x] **Live run screen** (`/sim/[id]/run`): the demo Stage 03 grammar made real — agent-network canvas (8 labeled leads, discipline clusters, 110-resident outer ring, pulse lines, speaker highlight) + threaded forum feed (POST n / REPLY / CHANGED POSITION tags, burst dividers, typing indicator, autoscroll) + DAY n/14 clock, progress bar, PAUSE / SPEED 4× / SKIP TO END / REPLAY — driven by the **Site 47-A golden fixture** (`lib/replay-fixture.ts`, 46 events extracted verbatim from the demo); the engine swaps in real events over the same shapes
- [x] **Engine v1 — all seven modes live** (`lib/engine.ts`, TS orchestrator in-app; §6.3 deviation noted in CLAUDE.md — the Python/swarms service remains the scale path): `compilePersonaPrompt()` (§6.1, pure), Agora (priority/round-robin/random/mention-driven speaker selection via cheap router), Roundtable rounds, Tribunal sides + judge's notes, Chamber takes→blind review→chair synthesis, Jury scored verdicts, Desk assignment→sections→memo, Expedition five phases; **corpus grounding with citations** on every lead turn (Files-API blocks + cached prefix — posts cite documents by name); **crowd sentiment polls** between rounds (batched Haiku, distribution + in-character quotes); stability convergence check; every post/event persisted (§6.2 shapes, migration 0012) and streamed ND-JSON; runs resumable from the transcript view
- [x] **Live run screen** (`LiveRun`): LAUNCH/RE-RUN from the page, per-mode canvas arrangements (ring · tribunal benches + judge · jury lanes + tally · desk tree · expedition route), per-mode feed structures (round markers, ARGUMENT/REBUTTAL columns + judge cards, VERDICT cards with parsed scores, phase dividers, memo assembly), **expandable crowd-poll cards** (distribution bar + in-character quotes — the real-run burst, nothing hidden), citation chips on posts, presence/typing, transcript reloads from the database on revisit
- [x] **Report engine (§8) + verifier (§4.1)**: one synth pass (tier synth model — Opus on standard) compiles the structured report from brief + transcript + polls — verdict chip with tone, executive summary, 4-6 dimension scores, one cited finding per question-to-resolve, risk register with watch signals, **preserved dissents verbatim** (adversarial voice always represented), tripwires, computed methodology + limitations; **verifier pass** checks numeric claims against the corpus (supported/contradicted/silent + contradiction cards); stored versioned in `reports` (migration 0013); `report.synthesize`/`report.verify` logged
- [x] **Reports are permanent run records**: nothing deletes them — re-cast and re-run leave every report intact; each report **freezes its full run at synthesis** (transcript, cast with provenance/adversarial tags, and run parameters live inside the report spec), so citations keep working forever and any run is reproducible from its report; **V1/V2/… version switcher** on the report page walks the history — the simulation becomes a reusable template (brief + corpus stable, vary modes/personas/params per run)
- [x] **Report viewer** (`/sim/[id]/report`, demo Stage 04 grammar): verdict chip, stat tiles, grow-bar scores, findings with **POST n citation chips that jump to the anchored transcript** (flash highlight), risk table, warn-bordered dissent cards, tripwire list, methodology block; RUN COMPLETE banner gains “Synthesize the report →” with live stage progress; workspace stage 05 REPORT goes live; **Reports** sidebar tab lists every report (verdict-toned cards)
- [x] Run UX round: run-config numbers are **stepper controls** (−/+ with free typing, commit on blur/Enter — the old inline component remounted per keystroke and dropped focus); empty run feed shows a centered **▶ Launch the run** hero; crowd materialization renders a full-size swarm progress block with a live counter and the canvas ring grows as members land; sidebar sub-menu retired in favor of dashboard **search + ALL/RAN/NOT-RUN/HAS-REPORT filters + 12-per-page pagination**; convergence hardened (stability stop needs round ≥ 3 + two consecutive strict verdicts; personas hold positions instead of drifting agreeable)
- [x] Feedback round: brief textarea grows without clipping (long statements drop a size); **bulk uploads queue visibly and run 3-wide** (serial loop made 5-file drops look stuck); poll animation reverted to the counting sweep (the radiating-rings take lost the plot); **Proceed-to-launch flushes the run config before navigating** and the engine streams a `config` event so ROUND n / N always shows the real cap; feed↔canvas sync deepened (steady reply lines to the actual answered author, composing ring while an agent thinks); one launch control at a time (hero on empty feed, RE-RUN after); **synthesis is a streaming progress strip** (friendly stage notes, no model names, report link only when the report exists → "Read the report →"); stale-cache self-heal on the run screen (complete-but-empty refetches); stage crumbs reflect real artifacts (04 RUN = persisted posts, 05 links the report); **Reports tab: dates, search, verdict filters, pagination, hover ⋯ delete** (per-version or all — `DELETE /api/reports/[id]`, RLS from 0013); report synthesizer must **commit to an answer** when the brief demands one (conditional reserved for named blockers; option briefs name the winner in the verdict chip)
- [x] **Mode integrity round**: **Jury is now multi-round** (§5 MixtureOfAgents layers — round 1 scores blind, later rounds see a code-computed TALLY post and re-score hold-or-move; convergence = scores stopped moving, computed in arithmetic not by a judge; crowd polled every round); **honest stop reasons** everywhere (`stop_reason` in run_result + report methodology: stability | rounds | budget | choreography — fixed-shape Chamber/Desk/Expedition say "phases complete", never "converged"); Chamber polls the crowd after takes and after synthesis; **casting fit gate** — library FTS matches below strong lexical overlap must pass a Haiku "could this person credibly hold this seat?" check before seating (kills Wine-Cellar-Builder-as-Pool-Specialist; rejects become generated personas; `casting.fit` logged); persona prompts carry the **seat mandate** ("the panel seated you as its X — speak with that seat's authority"); **mode-switch honesty** — a persisted transcript displays under the mode that produced it, with a note when the configured mode differs, and RE-RUN is a two-step confirm ("replaces this transcript; reports keep their frozen copy"); run config hides ROUNDS/STOP-WHEN for fixed choreographies and prices each mode's real post shape; **reports deliver the success criteria** — sections for uncovered criteria + a ✓ delivery-receipt checklist mapping every criterion to where it's answered
- [x] **Home dashboard + one-mode-control flow**: Home becomes a real overview once there's activity — clickable **stat tiles** (simulations/running/complete, reports, personas, conversations, 14-day model calls + tokens), a **14-day activity bar strip** (grow-bar grammar, links to Monitoring), **in-progress runs** pinned with a pulse, recent simulations with RAN badges, latest reports as verdict-chip rows, personas and conversations; **the interaction mode is chosen in exactly ONE place** — the animated cards in CONFIGURE THE RUN — population's duplicate mode pill row removed, the director's pick seeds the selection and stays tagged **✦ DIRECTOR'S PICK** (`casting.recommended_mode` survives user changes), re-casts re-seed the picker live via `onModeChange`, and fit flags + FIX-THE-CAST cover mode/cast mismatches; **hand-pick gets the full-library browser** (`/sim/[id]/cast` — ParticipantBrowser generalized: filter rail, smart search, pagination, multi-select tray, "Seat N on the panel"); Chamber take-sameness fixed (rotating opening angles + a no-stock-openers forum rule); **Monitoring rows carry engine context** (engine.turn logs agent/seat/mode/round/tag; polls, router, converge, casting.fit, report calls all log detail — expanded rows render CALL CONTEXT key-values); typed questions-to-resolve keep their **full text** (word-boundary chip label + wrapped framing everywhere, composer and workspace); pre-cast composition pills locked to one line
- [x] **Panel robustness round**: casting can no longer error on truncation — 9,000-token plan budget, lean seat JSON (why ≤12 words), and a **continuation top-up** that asks for only the missing seat definitions and merges (partial-but-usable beats an error; hard failure only when zero seats parse); **gap generation runs in concurrent chunks of 4** with cross-chunk name de-collision (the serial 7K-token generation was the slow tail on big panels); **manual questions get AI-summarized chip labels** (`/api/brief/label`, Haiku — instant provisional label, summary patches in) and question rows use a **fixed 208px label column** so every framing aligns (composer + workspace); workspace widened to 1060 and composition pills shortened — no hidden scroll; **hand-pick roster rail** (collapsible: everyone selected, profile click-through, remove) with **LEAD / CROWD assignment** per pick — the agents API seats crowd picks as real-persona crowd rows (`crowd-*`, seat.tier crowd, polled by the engine, lit on the CrowdBand); hand-picked panels get an explicit **✦ AUTO-CAST INSTEAD** revert; DIRECTOR'S PICK is now a single ✦ with a hover story (no squished pill); run-screen problem statement expands to the **full brief** on click; **REPORT LENGTH control** (✦ AUTO · BRIEF · STANDARD · DENSE) flows config → synth prompt depth rules + token budget (auto scales to the transcript: ≤12 posts brief, 40+ dense)
- [x] **Bulletproof the core (next-level plan Phase 1)**: **offline engine test matrix** — vitest + a scriptable FakeAnthropic injected through `EngineContext.anthropic` ([tests/](tests/)): all seven choreographies × stop conditions with exact post counts and honest stop reasons, suspend/resume (mid-round, the Tribunal judge-skip poll regression, mid-poll partial tallies, polled-round dedupe), failure injection (empty-turn loud-fail + retry, garbage/thrown poll batches, judge and router fallbacks), Jury arithmetic extracted to exported pure helpers (`juryScoreOf`/`juryScoresAt`/`juryMovement`/`juryTallyLine`) and pinned, `compilePersonaPrompt` snapshots (seat mandate, anti-stock-opener, temperature bands) — 41 tests, <200ms, zero tokens (`npm t`); **empty-turn escalation ladder** — an adaptive-thinking model that spends its whole output budget thinking and returns zero prose now retries at 3× then 6× the ceiling instead of failing at the same budget twice (the "Angela F.'s turn failed twice" production failure; poll/judge/router budgets got the same headroom); **round-semantics table** documented once in README + `/docs` Interaction modes; **SIM DAYS removed** (dead control — the engine never read it; old configs tolerated on read); **canvas labels** now first name + last initial, matching the feed byline; live smoke sweep script (`scripts/smoke-live.mjs`) runs all seven modes end-to-end against a real session
- [x] **The living forum (next-level plan Phase 2 a/b/c)**: **threaded reply chains** — replies target any post from the round (weighted recent + contested, `pickReplyTarget` exported + pinned), reddit-style nesting in the feed (depth indents capped at 5, chain lines, per-subtree collapse "— HIDE N REPLIES"); **INTERACTION DENSITY control** (focused · lively · bustling — default lively): Agora replies ~1.5–2× leads, Roundtable CROSSFIRE half-round, Tribunal COUNTER volleys, all priced by the estimator **from the same `lib/run.ts` density functions the engine runs** (killing the estimator/engine drift for good); **crowd interjection bursts** — a density-scaled crowd sample posts short in-character reactions threaded under the round's posts, one batched call per burst; **votes (§2b)** — migration `0014_post_votes`, per-round in-character vote pass (leads + crowd sample, batched ~20/call, never on their own post), ▲/▼ chips with hover attribution, accent ring on each round's most-endorsed post, vote totals fed to the report synthesizer as citable signals; **Take the Floor** — the user posts into the forum (`POST /api/simulations/[id]/floor`), @mention typeahead over the panel, mentioned agents reply with transcript + corpus + persona context, user posts render "YOU · TAKING THE FLOOR" and are citable; **roster rail** — "N ON THE PANEL →" collapsible rail with seat/discipline/stance cards → full PersonaProfile; clicking a canvas node opens the same profile; offline matrix extended to 63 tests (threading invariants: every reply_to resolves earlier + no cycles; density counts; burst/vote resume dedupe; floor routing)
- [x] **Forum-quality round** (field report from the first lively Agora run): **anti-repeat guard** — a draft that substantially restates the speaker's earlier post this round (`textSimilarity` ≥ 0.8) gets one explicit do-not-restate retry, and a failed retry keeps the draft instead of killing the run; **speaker guardrails** — never the same voice twice in a row, never a self-reply, and coverage beats relevance (a hot hand with 2+ posts yields the mic to leads who haven't spoken — every lead speaks in a lively round); **realtime votes** — a rotating 6-voter sample reacts every 3rd post at lively/bustling so ▲/▼ chips move mid-round, pair-deduped against the closing sweep; **scroll-position-aware feed** — scrolling up holds your place while posts stream, with a "↓ N NEW POSTS — JUMP TO LATEST" pill back to the live edge; smoke sweep gains live invariants (all leads spoke, zero consecutive same-author posts, ≥2 vote events)
- [x] **Report synthesis robustness** (field report: a 78-post dense Agora failed with `unparseable synthesis (stop: max_tokens)`, and its re-run shipped a GUTTED report — verdict only, findings/criteria/risks empty, because bracket salvage "successfully" closed a truncated JSON): synthesis now runs the same **escalation ladder** as engine turns (8K/16K/24K starting ceilings by depth → ×2 → ×3 capped at 48K — adaptive thinking bills against the ceiling); a `stop: max_tokens` draft is **never parsed**; a **completeness gate** (`reportSpecIncomplete`, exported + pinned by tests) rejects any spec missing verdict/summary/scores/per-question findings/criteria receipt/risks/tripwires and retries bigger instead of shipping partial; synthesis is **streamed** (keeps long Opus outputs inside HTTP limits) with live progress in the strip ("COMPILING… ~N WORDS DRAFTED", honest 1–3-minute note on dense) — 76-test offline suite
- [x] **Report excellence (Phase 3a)** — audit of every generated report found the prose faults (findings led with mechanism not answer; 190-word chained exec-summary sentences; analyst labels replacing the user's questions; unexpanded jargon) and the fixes ship as contract, not vibes: **THE BOTTOM LINE** opens every report (the answer / what would change it / do next — jargon-free by rule), every section carries a **direct bolded answer** to the question AS THE USER ASKED IT plus a **numbers-that-matter chip row**, CLARITY RULES in the synth prompt (answer-first, ≤25-word sentences, acronyms expanded at first use), and the completeness gate enforces bottom line + per-section answers so a report can't ship without them; **SIMPLIFY toggle** — one cached translation pass over the frozen spec (`spec.plain`, own schema + gate + glossary, `reports_update` RLS in 0016; a translation can never disagree with the expert view, and it works retroactively on old reports); **crowd-sentiment round SLIDER** — one set of bars you scrub through the rounds (with per-round deltas and an expandable percentages table), rendered from the existing spec data; **Monitoring full history** — total count + "LOAD 500 OLDER" cursor paging past the response window (the table was silently capped at the newest 500); amended Phase 3 plan written into docs/next-level-plan.md (stress tests parked, forks to backlog)
- [x] **Poll instrument + true simplify (Phase 3a follow-up)** — the crowd poll now asks an **explicit brief-derived question** (one Haiku pass at launch, persisted in `config.poll_question`, shown on every run-screen poll card, the report slider, and the simplified crowd section — "62% support" finally has a referent); polls are **transcript-aware** (each round's poll carries a digest of what the panel just argued, so movement is reaction, not sampling noise); **disengagement fixed** — the four stances are defined in the prompt (disengaged = genuinely unaffected, never a stand-in for neutral) and `normalizeStance` maps variants ("supportive", "against", "undecided") to their real bucket instead of the old blanket coercion into disengaged; **SIMPLIFY is a different page now** — answer-as-headline hero, plain Q&A cards, STRONG/MIXED/WEAK word grades instead of decimal score bars, card risks, everyday crowd labels, one-line "how this was made" — not the expert layout with swapped strings (10 new offline tests pin the prompt, the normalization, and the event contract)
- [x] **Files you can point at (PR-A of the field-report batch)** — fixed the "describe 4.jpg — not found" bug at the root: images were attached to the model as ANONYMOUS blocks (PDFs carried titles; images carried nothing), so filenames could never resolve. One shared `buildCorpusBlocks` (lib/corpus.ts) now feeds corpus Q&A, the engine, Take the Floor, and synthesis — every image gets a label block with its filename + upload ordinal ("UPLOADED IMAGE 2 OF 5: 4.jpg"), making both "4.jpg" and "the second uploaded image" resolvable. Workspace UX: image uploads get row THUMBNAILS + a click-to-view lightbox, and the Test-the-corpus box gains an **@file typeahead** (type @ → pick a file → the exact quoted filename is inserted). **Reports can now SHOW decision-critical uploads**: the synthesizer picks the files the decision turned on (`media` — exact filenames only, resolved against real uploads, unknown names dropped), and the report renders a KEY MATERIALS section — the winning listing photo displayed inline with a caption, documents as signed-link cards — in both expert and SIMPLIFY views (signed at view time, never stored)
- [x] **Agent tools v1 (Phase 3d)** — the extensible TOOL RACK (`lib/tools.ts`, one descriptor per tool: name/tagline/description/example/cost/status/wiring — future tools are new entries, never re-architecture): **Web research** ships available (Anthropic server-side web search, variant per model tier from the rack, max 2 searches/turn) beside greyed-out COMING SOON cards (parcel · econ series · census · historical web · flood/climate). **All OFF by default**; simulations allowlist via the AGENT TOOLS card section in run config (multi-select, ENABLE ALL/DISABLE ALL, usage-based estimator line) → `config.tools`; **Conversations get per-participant tools in the same menu as the model tier** (chip shows 🔎 when enabled; `tool_overrides`). Use is **agent-decided** (addendum only when enabled: "search only when it would change your answer") and LEADS-ONLY in sims — crowd polls/interjections/votes never carry tools. Every search: 🔎 feed card with clickable sources (live + replay + 3c observer), `tool_runs` + `tool` events (migration 0017), **shared factbase** ("facts the panel already pulled" digest — nobody re-searches), report TOOL FINDINGS input + TOOL CALLS tile + WEB SOURCES appendix + methodology line, and chat bubbles show "🔎 SEARCHED" + source pills. Failures soft — a dead search never kills a turn. 9 new offline tests (registry invariants, default-off, leads-only, event shape, factbase, pinned post counts with tools on) + smoke: default runs must show ZERO tool events, a current-rates brief with tools on must search
- [x] **Runs you can walk away from (Phase 3c)** — the engine no longer lives inside the response stream: the launch route validates and kicks the slice worker off under `waitUntil`, the stream is a detachable WINDOW (closing the tab never cancels a run), and suspended slices **schedule their own successors** through a secret-authed internal `/run/continue` route (HMAC of the service key — nothing new to provision; no service key → clean fallback to client-driven continuation). `config.run_state` + a 12s heartbeat are the truth: launch/continue refuse to double-drive a fresh-heartbeat run, a null heartbeat is a between-slice handoff, and a long silence marks the run orphaned with a one-click RESUME. The run screen becomes an **observer** when it isn't the driver — landing on a running sim (yours or the Home in-progress strip's) tails the persisted transcript live with dedupe across stream↔observer handoffs. **Graceful STOP**: `/run/stop` sets a flag the worker's truth loop picks up, zeroes the engine deadline so it suspends at the next safe boundary, and the run completes honestly with stop reason "stopped" — transcript preserved, report synthesizable ("HOW IT ENDED: STOPPED"). Smoke gains the acceptance test: first post → kill the stream → the run must finish alone; then a second run stopped mid-flight (110 offline tests incl. chain-secret + heartbeat rules)
- [x] **Headlines that fit the question (Phase 3b)** — the report lead is TYPED (`spec.lead{kind,…}`): **decision** keeps today's verdict chip; **price range** leads valuations with a defended band (low/high/central estimate, warn-colored walk-away marker, triangulation basis line); **approval odds** leads hearings (committed %, likely/toss-up/unlikely band meter, the 2-3 drivers that move it); **key finding** is the universal catch-all — any other brief gets a committed most-important-conclusion headline with so-what + magnitude chips, so nothing falls outside the system. Synthesizer picks the kind from the brief (+ the silent `brief.template` as a hint), the completeness gate rejects uncommitted leads (a range without numbers, odds without a number), assembly carries every field with clamps, and pre-3b reports render exactly as before. Reports tab + Home outcomes gain an exclusive **INSIGHT** bucket (non-decision leads show their metric — "$4.2M–$4.6M", "68% LIKELY" — instead of a forced GO/NO-GO chip)
- [x] **Casting fit-gate fix + seat↔person clarity** (field report: a Foot-Traffic Analyst seated as "Retail Leasing Broker") — the fit gate's lexical bypass counted tagline/skills words, which are exactly the words FTS surfaced the candidate on, so lookalikes skipped the Haiku judge; the bypass is now `roleOverlap` (seat title vs the persona's OWN title, nothing else) and org-persona matches face the same gate (previously ungated); the UI now shows the seat↔person relationship instead of hiding it — cards add a **BY TRADE** line and the profile a **CAST AS** line whenever the seat title differs from the persona's own title (8 new tests pin the Valentina regression, the near-fit case, and `seatRoleDiffers`)
- [x] **Agent tools v1 (§7 Phase 1)** — shipped as Phase 3d (PR #58): the tool rack (`lib/tools.ts`), web research on lead turns (agent-decided, ALL OFF by default, `config.tools` allowlist + run-config cards + per-participant tools in Conversations), searches as `tool` events → `tool_runs` → shared factbase → report TOOL FINDINGS/appendix
- [x] **Polls that fit the question (PR-B of the field-report batch)** — the crowd poll instrument now matches the brief: choose-between briefs ("which of these three photos leads the listing?") poll the brief's ACTUAL alternatives (derived once at launch with the question — `derivePollInstrument`, options persisted in `config.poll_options`; the classic support/conditional/oppose/disengaged stance poll stays the default for proposition briefs), each polled member picks one choice verbatim (unique-containment normalization; honest UNDECIDED bucket for non-answers), and sentiment events carry the option list so the run-screen poll cards, the report's round slider + percentages table, and the SIMPLIFY crowd section all render the real choices ("44% green.png · 31% red.png") — old runs untouched. Rider (field-report item 4): the **synthesis ticker** — the report draft streams schema-shaped JSON, so the progress strip now reads the buffer and narrates the actual milestone ("✓ SUMMARY · WRITING FINDINGS 3/6 · ~1,240 WORDS · 1:07") instead of a bare word count (`lib/synth-progress.ts`, pure + pinned by tests)
- [x] **Corpus Q&A answers any upload** (field report, PR #61) — killed the images-only refusal (`corpusQaSystem`: every upload is evidence; an image's filename reference IS its citation; answer the question asked, the brief is background); @file typeahead tokens hold their typed form (`@green.png`, quoted only when the name has spaces)
- [ ] **Field-report batch 2** (testing pass 2026-08-04, plan in `docs/next-level-plan.md`) — **PR C**: poll percentages sum to exactly 100 (largest-remainder rounding) + legible legend rows; SEE EVERY VOTE expander (full per-member poll responses persisted on the sentiment event); markdown rendering in the forum feed; a real SIMPLIFY loading animation (the "blue spinner" was `cursor: wait`); plain mode refers to files by exact filename; the report **file rail** (every upload with IMAGE-n ordinal chip, thumbnail, lightbox — the same ordinals agents see); KEY MATERIALS as a uniform 3-up grid. **PR D**: detached report synthesis (waitUntil + `report_state` heartbeat — leave and come back, the ticker's still there) + SYNTHESIZING status on Home's in-progress strip
- [x] **3e — the forum acts like a real forum** — living threads: replies target ANY substantive post, this round at full weight and earlier rounds λ≈0.4-decayed, with a NECRO GATE (focused 0% — the pinned matrix untouched by construction — lively 25%, bustling 35%, recomputed from persisted posts so resume can't double-count); revivals carry an "update, don't reopen" order and a ↩ ROUND-n breadcrumb chip that jumps to the parent. **Parallel reply waves**: replies land in batches of 2–3 sharing one transcript snapshot (seqs pre-assigned, emitted in slot order — the feed stays ordered while generation overlaps), and Jury round 1 scores fully in parallel (blind by definition, juror order preserved). **Selective voting**: abstain by default with HARD per-voter budgets (endorse ≤2 / reject ≤1 per round, enforced in code against greedy voters, shared by the realtime layer and the close), plus the retro slice — old posts revived by new replies re-enter the close's ballot and votes flip latest-wins. Pill becomes "↓ GO TO BOTTOM · N NEW". Also fixed in passing: the speaker-guardrail fallback indexed modulo the panel size instead of the filtered pool (could hand the mic straight back to the excluded voice)
- [x] **Field-report 3 — jury truth, file truth, run safety** — **choice juries**: a choose-between brief has no scoreable proposition (in the field all 10 jurors anchored the first upload and read "SCORE: 2/10" across three rounds); with a choice instrument, jurors now PICK one of the brief's actual options + a confidence score (parsed via `juryPickOf`/`normalizeChoice`), the TALLY counts picks and names the LEADER, and convergence = nobody switched (the instrument now also derives for crowd-less juries). **Mode truth**: reports label themselves with `run_result.mode` — the mode the transcript was actually produced under — never the current (possibly re-edited) casting config. **Ordinal collision**: "IMAGE n" labels/chips/rail are suppressed whenever any image filename contains a digit (IMAGE 1 = 3.webp vs the panel's "Image 1" = 1.jpg pointed at different files); the synthesizer is ordered to use exact filenames only. **Markdown**: bold containing nested *italics* renders correctly and unpaired ** markers are dropped, not shown. **Run safety**: a live run's settings are frozen — the config PATCH 409s while the run heartbeat is fresh and the workspace shows a SETTINGS LOCKED banner; after any fresh re-run the primary CTA is SYNTHESIZE (a pre-existing report is stale — offered as "read the previous run's report" only)
- [x] **Field-report 2 PR D — reports you can walk away from** (shipped, PR #68): synthesis under waitUntil with `config.report_state` heartbeat as truth; the response is a tail; a second POST attaches instead of duplicating; LiveRun re-attaches on return; SYNTHESIZING on Home's in-progress strip
- [x] **6-PR1 — the Brief Contract + the Understanding pass** (Phase 6 spine, `docs/next-level-plan.md` §6a/6b): a free-form brief (multi-part welcome, 6K chars) with **files attached right on the prompt** (`/sim/new` drop tray — same corpus pipeline, create-once retry safety); one CASTING_MODEL pass (`brief.understand`, salvage-parsed + gated) derives the **contract** — intent, audience, mirror prose, sub-asks with evidence standards, output contracts (ranked list/matrix/verdict/range…), entities, constraints, **population_hints** ("homebuyers aged 35-45 in Beverly Hills" IN the prompt → casting honors it; omitted → director decides), per-document **roles** (evidence/framework/question-source/reference), and ≤2 one-tap clarifier flags — persisted at `brief.contract`, rendered as the **WHAT I UNDERSTOOD card** (auto-derives once, editable chips, role cycling, RE-DERIVE, stale-flagged on brief edits and new docs). Back-compat: no contract → today's behavior
- [x] **Field fixes: runs never strand + the workspace tells one story** (2026-08-05, Adam's stuck-run report) — **(1) The walk-away guarantee is now server-side.** Root cause of the stuck run: 770s engine slices inside the 800s serverless window left 30s of kill headroom while a single web-search turn runs up to 137s — Vercel hard-killed the worker mid-call, no suspend fired, the heartbeat froze, and STOP set a flag no worker would ever read. Fixes: `SLICE_BUDGET_MS` 650s (150s headroom, offline-pinned); the **reaper** (`/api/engine/reaper`, Vercel cron every minute, `CRON_SECRET`-authed) sweeps zombie runs — stale heartbeat + pending stop → finalized immediately with the transcript; stale without stop → the slice chain re-fires via `/run/continue`; `/run/stop` on an orphan finalizes instantly instead of flag-and-pray; the run screen **auto-reclaims** an orphan once before falling back to the manual RESUME (`reaperAction` pure + pinned). The stranded production run was rescued live by re-firing its chain — the new worker honored the pending stop and closed it out gracefully. **(2) Simulation cards** lead with a NAME (your rename from the ⋮ menu, else the understanding pass's derived `contract.title`) + a clamped mirror summary; the full prompt collapses behind FULL BRIEF; RUNNING text became the pulsing ● LIVE dot. **(3) The workspace tells one story**: Test-the-corpus removed; files ride WITH the brief as overlapping circles + a "+" (details expand on demand; new files flag RE-DERIVE in warn); the **understanding theater** (narrated steps + progress bar) is all that renders until the reading lands, THEN population/run config reveal; report-shape pills now read "OPENS WITH · VERDICT / + TIMELINE / + MATRIX — all of these, in this order"
- [x] **Field fixes 2: run robustness for LONG tool calls + report/ranking truth + faster synthesis + wider waves** (2026-08-05, Adam's second field report) — **(1) No call can outrun the window**: the engine client now carries a hard per-request timeout (`ENGINE_CALL_TIMEOUT_MS` 170s, maxRetries 0 — the engine's own ladder retries at safe boundaries) and the slice budget dropped to 620s, making the walkaway invariant EXPLICIT and offline-pinned: `SLICE_BUDGET + CALL_TIMEOUT + margin ≤ 800s` — a turn stringing web searches together gets cut and retried safely instead of riding past the kill line (the reaper remains the backstop). **(2) Rankings are complete and never clipped**: the 11-of-11 guarantee — synth prompt gains the RANKING RULE (a brief that enumerates items to rank gets EVERY item an explicit position, "never debated" stated honestly); assembly caps widened (numbers 6×60 → 16×220, lead magnitude 60 → 200) with **word-boundary clipping** (`clipText`); ReportView renders long entries as full-width wrapping rows (short figures stay stat chips) — live-proven: an 11-category economy run ranked #1–#11 complete, wrapped, unclipped. **(3) Synthesis stopped double-billing itself**: the 4+ min report was TWO ~250s passes — a 22K-token dense draft truncated at the 24K ceiling and re-ran; dense now starts at 32K (ceilings are free, truncation is not) and the ladder dedupes — live: single pass, stop end_turn. Structural speedup (parallel Desk sections, §8) folded into 6-PR4. **(4) Wider reply waves**: `waveWidth` moves to lib/run.ts — lively 3 / bustling 4, ECONOMY +1 everywhere (focused keeps the serial v1 rhythm outside economy), capped at distinct eligible voices (a wave wider than the panel handed the mic back to a taken voice inside one snapshot — caught by the offline matrix, fixed structurally). **Phase 6 re-ordered: 6-PR3 → 6-PR4 → 6-PR2** — report/deliberation correctness before the Quick Run view
- [x] **6-PR3 — rounds that walk the brief** (§6c/§6d): with a Brief Contract, rounds stop being undirected — each opens with an **agenda** (round 1 = the full brief with every sub-ask named; middle rounds target the LEAST-RESOLVED sub-asks by name, quoting the tracker's "still missing"; the final round forces COMMIT), riding inside the opener instruction with choreography untouched. A **resolution tracker** (one Haiku pass per round close, `engine.tracker`) scores every sub-ask 0–100 → `coverage` events → the run screen's **COVERAGE strip** (chip per sub-ask filling toward resolved, warn→accent) + agenda labels on round dividers and the header. The **adaptive poll plan** (§6d): the Understanding pass emits ≤3 poll angles (proposition/choice, phased early/middle/late, `poll_plan` on the contract) — rounds are assigned contiguous blocks with ≥2-round persistence (`pollAngleForRound`), each sentiment event carries its own question/options/angle (zero schema change), the report renders **one trend slider per angle**, and an EMPTY plan polls NOT AT ALL (crowd still interjects + votes; the card says NO CROWD POLL plainly). WHAT I UNDERSTOOD shows the plan as phase-tagged chips. Resume-safe end to end (trackedRounds/coverage rebuilt from persisted events); back-compat: no contract → no agendas, no tracker, the legacy single instrument — pinned byte-identical
- [x] **6-PR4 — reports that take the answer's shape** (§6e/§6f): **blocks** — contract-driven answer ARTIFACTS in one flat schema shape (`ReportBlock`, 3b schema-budget lesson) rendered three ways: RANKED LIST (numbered wrapping rows, every enumerated item placed, per-row rationale + cites), MATRIX (a real entities×criteria table, horizontally scrollable), COMPARISON (side-by-side pros/cons/bottom-line cards) — required via `blocksSpecFor(output_contracts, entities)` in the synth prompt, normalized with word-boundary clipping, shown in BOTH views. The **answer-completeness judge** (`report.judge`, TIER_MODELS.judge — never below Sonnet): after assembly the draft's ANSWERS are checked against the CONTRACT (every sub-ask answered-not-mentioned, artifacts complete, evidence standards met, criteria receipts real); failures trigger ONE targeted repair pass (`report.judge_patch` — regenerates only the failing sections/blocks, merged in place by question/kind), and the receipt prints in METHODOLOGY ("passed" / "N pieces repaired" / honest unfixed-gaps warning). The **audience register** (§6f): an executive-audience contract eager-generates the plain translation inside the detached pipeline (shared `synthesizePlain`, also used by SIMPLIFY) so the report OPENS in the plain voice instantly — one click flips to technical; deliberation untouched. Back-compat: no contract → no blocks demanded, no judge, technical default. (Parallel Desk-section synthesis + the ranking/matrix/free-probe CROWD instruments move to **6-PR4b** — synthesis restructure and instrument-artifact pairing land together, isolated from this correctness batch)
- [x] **6-PR2 — QUICK RUN** (§6a DECISION, one-box alternate view): a ⚡ toggle on /sim/new (preference persists per user; CLASSIC stays the default) opens the one-box composer — empty state is the box + file drop; once typing starts the config reveals progressively: the animated **mode cards** first (AUTO = the director decides + the seven ModeDiagram cards), and clicking a mode shows its params as **selection pills**, mode-aware (fixed-shape modes hide ROUNDS). The prompt may DESCRIBE the population ("homebuyers aged 35-45 in Beverly Hills…") — the Understanding pass extracts it and casting honors it. NO population stage: RUN (estimate ON the button — director's-typical-panel basis, live-reactive to pills) drives create → files → understand (one-line **✻ UNDERSTOOD** receipt: intent · sub-asks · poll plan · population-from-your-prompt) → cast (seat-by-seat status lines) → straight into the live run via ?autostart=1 — LiveRun's proven path materializes the crowd and launches (never over an existing transcript, so reloading the URL can't double-launch). Retry-safe: a failed step reuses the created sim
- [x] **6-PR4b — parallel Desk-section synthesis** (§8): briefs with ≥3 questions synthesize as outline → **parallel section workers** (one per question, 4-lane pool, `report.section`, escalating ceilings) → a **DIRECTOR pass** (`report.director`, `REPORT_DIRECTOR_SCHEMA` = the full schema minus sections, DERIVED so the two can't drift; prompt variant instructs consistency with the provided drafts, never re-emission) → merge + the same completeness gate. Any failure (worker, director, gate) falls back to the single-pass ladder honestly. Live: 4 sections drafted in 27s wall-clock + 127s director = 229s total WITH judge + repair + eager plain — and the judge's repair pass fired live for the first time. Crowd instruments (ranking/matrix/free-probe) ride with 6d-2 cohorts
- [x] **Field fixes 3: file circles own file management** (Adam's pre-#80 field report) — the leftover DILIGENCE MATERIALS card is gone entirely (the brief's overlapping circles + "+" are the ONE file surface; field notes open as a standalone editor; IMAGE-n ordinals ride the circle tooltips, the 1M-context-tier note rides the count line); circles gain a **hover "−" badge** to remove a file; the understanding card's re-derive warning is now **symmetric and state-true** — docs ADDED since the reading, docs the reading USED that were removed, and brief edits each warn specifically, and add-then-remove returns to the original state with NO warning (live-proven end to end)
- [x] **Field fixes 4: searches never strand + unread-report badge + full live-run lock** (Adam's Chamber field report) — **(1)** a web search now ALWAYS rides inside the post it informed: the run screen's observer tail polls posts before events, which could deliver a live search AFTER its post and strand it as a standalone WEB RESEARCH card — attachment is now order-independent (`computeToolAttachment` in lib/feed.ts, pure + pinned: forward to the agent's next post, else BACKWARD to their latest post in the same round; the card remains only for truly orphaned searches whose turn failed). **(2)** the Reports sidebar item carries an **unread (n) badge** — org-scoped report ids vs a local seen-set stamped when a report opens; soft 45s poll + focus refresh, clears live via an in-tab event. **(3)** while a run is LIVE the WHOLE workspace locks (brief, files, understanding card, population, config — one dimmed surface with the warn banner) and the primary action becomes **View live run →** (top banner + bottom button, same destination as the RUN breadcrumb)
- [x] **Run-finished badge** (Adam's follow-up to #83): the Simulations sidebar item now carries the same green **(n)** for RUNS that completed while you were elsewhere — org-scoped `/api/simulations/finished` (status complete + `run_result.at`, 7-day window so a first load never floods) vs a local `{simId: seenAt}` map stamped whenever the run screen shows a finished run; a RE-RUN's newer `at` makes the sim unread again; 45s poll + focus refresh + in-tab clear. Together with #83's Reports badge, both halves of "notify me when it's done" are covered: run done → Simulations (n); report synthesized → Reports (n)
- [ ] **⟶ NEXT: 5a national PUMS substrate** (all 50 states + DC + PR, ACS 5-year, latency-first) — Phase 6 remaining: 6d-2 named cohorts + the ranking/matrix/free-probe crowd instruments
- [ ] **Then Phase 5 (grounded crowds & the data layer, see `docs/next-level-plan.md`): 5a ACS PUMS census-grounded crowds (NATIONAL — all 50 states + DC + PR, 5-year vintage, slim columns, millisecond weighted sampling + precomputed strata rollup) → 5b describe-to-create personas (PUMS-backed autofill) → 5g cast-a-room (premise-cast group chats: "14 buyers who could afford this $3.2M Beverly Hills listing" → a PUMS-grounded room you just talk to, with one-click POLL THE ROOM) → 5c data tools (FRED · Census · HUD · FEMA · Regrid + finance-calc) → 5d scenario forks + report diffs, with 5e calibration v0 and 5f valuation scaffold riding alongside
- [ ] **Phase 1 exit:** Site 47-A reproduced live, unscripted, <15 min wall-clock, <$25/run, by a signed-in user whose run persists

### Phase 2 — Seeding depth
- [ ] Multi-metro PUMS pipeline + seeding corpus tier 2 (AHS, GSS/Pew, NAR, migration, licensure)
- [ ] Persona editor + persona sets; embeddings-based library matching (Vercel AI Gateway)
- [ ] **Land & parcel valuation use case** (flagship decision shape): valuation seats (appraiser, land broker, developer, adversarial skeptic), method triangulation (sales comps · residual land value · income cap · highest-and-best-use), comp-package grounding with verifier-audited numbers, defended price *range* + tripwires in the report; calibrate against actual trades via `outcomes`
- [ ] **Agent tools & data expansion** (smartest agents for every real-estate question): Phase 2 free tools — county parcel/assessor (Regrid-class), Census/ACS, FRED/BLS, HUD FMR, FEMA flood, OSM/GTFS; Phase 3 commercial — ATTOM/CoreLogic comps, CoStar-class CRE, AcreValue-class farmland, third-party AVM outputs as panel *inputs to argue with*, permits, foot traffic
- [ ] Cohort sentiment polling between rounds; convergence detection
- [ ] Scenario forks + report diffs (`simulations.parent_id`)
- [ ] Take the Floor: user posts into live runs; Ask-the-panel follow-ups on reports
- [ ] Conversations v2: docs through the corpus pipeline, in-thread chart generation, opt-in cross-thread memory

### Phase 3 — SaaS
- [ ] Multi-user orgs, invites, roles; shareable report links
- [ ] Billing: run-based credits + subscription tiers
- [ ] Tool phase 2: Census/FRED/HUD/parcel/flood/OSM live tools

### Phase 4 — Marketplace + calibration
- [ ] First-party persona marketplace (review gate: quality + fair-housing)
- [ ] Outcomes tables in anger; first published backtest

## The value

**The unit of work it replaces.** Decision-grade feasibility research runs $15–50K and six weeks per question. A Microcosm run is same-day, and the marginal research question becomes nearly free. Pricing follows a barbell the market has already validated:

- **Team tier (~$18K/yr)** — unlimited standard runs, priced below a single consultant study. A no-committee purchase with thousands of potential buyers.
- **Enterprise tier (~$250K/yr)** — the calibrated metro twin, the underwriting API, and the accuracy record. Sales-led, like every six-figure enterprise simulation deal in the category.
- **Public channel (~$120K/engagement)** — intervention studies for cities and pro-housing funders, on procurement rails Replica already proved (MTA, State of Illinois).

**The market.** ~$14B/yr global real estate consulting (market research and due diligence are its two largest segments), inside a $150B+ insights industry and a $198B US real estate services market.

**The moat.** The generative-agent layer is commoditizing — the moat is vertical: a real estate ontology baked into personas and reports, calibration against *actual* sales curves, lease-ups, and hearing outcomes, and embedding in underwriting workflows. The outcome corpus compounds; the agents don't.

### Honest limits

Synthetic populations are strong on stated preferences, message tests, directional ranking, and anticipating objections. They are weak in documented ways — sycophancy, compressed tails, WEIRD bias, optimism-skewed preferences — and Microcosm's position is to model the say-do gap explicitly, publish backtests with error bars, and disclose every output as synthetic and directional. Fair-housing review gates any use case near tenant or buyer selection. The simulation informs the decision-maker; it never replaces them.

## Running locally

```bash
npm install
npm run dev        # http://localhost:3000
```

Secrets live in `.env.local` (gitignored): `ANTHROPIC_API_KEY`, Supabase URL/keys, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`. The app runs unauthenticated pages without them, but auth, Conversations, and the library need Supabase + Anthropic keys.

One-time / occasional scripts:

```bash
node scripts/generate-personas.mjs --dry     # parse the taxonomy, count seats
node scripts/generate-personas.mjs           # generate & insert missing library personas (idempotent)
npx supabase db push --password $SUPABASE_DB_PASSWORD   # apply new migrations
```

Working in **Claude Code**? A preview config ships in [`.claude/launch.json`](.claude/launch.json) — the `microcosm-app` server starts on port 3000 straight from the Browser pane.

## Repo layout

```
├── app/                    # Next.js routes
│   ├── page.tsx            #   landing
│   ├── login/              #   Google-only auth
│   ├── (app)/              #   authed app: dashboard, conversations, personas, monitoring, settings
│   ├── api/converse/       #   group-chat orchestration (routing, replies, attachments, logging)
│   ├── api/personas/search #   LLM smart search over the library
│   └── sim/demo/           #   golden-fixture run screen
├── components/             # Nav, Hero, app shell, Conversations, PersonaManager, RunScreen…
├── lib/                    # personas + prompt compiler, chat model tiers, supabase clients, event schema, fixtures
├── engine/                 # Python FastAPI + swarms simulation service (skeleton)
├── examples/               # 7 demo-ready simulations, one per mode: paste-ready briefs, uploadable diligence docs, run configs (examples/README.md)
├── scripts/                # generate-personas.mjs (persona foundry)
├── supabase/migrations/    # schema, RLS, conversations, monitoring, library search, model overrides
├── docs/                   # persona taxonomy (~1,100 base personas), speaker script
├── public/                 # demo.html + support.js (golden fixture), hero video
├── legacy/                 # archived original static pages
└── CLAUDE.md               # the product/tech/design spec — read before coding
```

## Status

Internal preview on production infrastructure — auth, persistent conversations, the generated persona library, smart search, and monitoring are live for Fifth Wall accounts. The current build target is the Phase 1 exit: reproducing the Site 47-A demo live and unscripted (see the [build checklist](#build-checklist)). All simulation outputs shown are illustrative and disclosed as synthetic.
